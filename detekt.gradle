import dev.marlonlom.apps.glucoreo.buildsrc.Plugins

apply plugin: Plugins.detektPluginId

detekt {
  parallel = true
  toolVersion = Plugins.detektPluginVersion
  input = files("src/main/kotlin")
  config = files("$project.rootDir/.detekt/detekt.yml")
}
